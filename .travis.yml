sudo: false
language: java
jdk: openjdk8

install: true                   # Skip install so we avoid running phases twice

script:
  - mvn --show-version --batch-mode -Dmaven.javadoc.skip=true integration-test
