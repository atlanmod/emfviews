Before executing  your Epsilon Generation Language make sure that your run configuration resembles
the one depicted in figures runConfigurationStep1.png and runConfigurationStep2.png

Note for figure 2: After you select your view (myPublicationsAndBooks.eviewtype),
the run configuration will automatically load  in the metamodels. However you have to
manually remove it using the Remove button and select the corresponding viewtype
(publicationsAndBooks.eviewtype) using the Add file button. 